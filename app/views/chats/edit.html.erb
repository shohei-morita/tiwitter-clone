<h2>つぶやきを編集する</h2>
<div class="form-group">
  <%= render 'form' %>
  <%= form_with(model: @chat, local: true) do |form| %>
  <%= form.submit 'だまる', name: 'back', class:'silence-btn btn-default'%>
  <% end %>
</div>
